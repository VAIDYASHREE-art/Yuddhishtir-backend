# render.yaml
services:
  - type: web
    name: yuddhishtir-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PORT
        value: 8000